export enum periodType {
  DAY,
  WEEK,
  MONTH
}

export enum bodyType {
  ENDO,
  EKTO,
  MEZO
}

export enum intensityType {
  LIGHT,
  MEDIUM,
  HARD
}

export enum ActivityType {
  AERO = 'AERO',
  GYM = 'GYM'
}
