export enum SelectDayMode {
  SINGLE, // only one selected date
  HALF_DOUBLE, // after select start date END date can be only date after start. User cannot choose eariel date than start
  DOUBLE, // after select eariel date than start date dates arnge are switched.
}
